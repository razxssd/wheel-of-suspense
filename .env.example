# Upstash Redis — backs the shared saved lists & winners history.
# Provisioned automatically when you add the Upstash integration on Vercel
# (`vercel integration add upstash`), then pulled with `vercel env pull .env`.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
